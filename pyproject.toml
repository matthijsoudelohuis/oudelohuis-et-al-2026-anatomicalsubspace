[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "molanalysis"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["cca*", "doc*", "labeling*", "loaddata*", "rrr*", "utils*"]
